# Bright Data scraper screenshots. Kept locally as the source of truth for
# dataset_ids + field_maps, never committed: every screenshot's "Code examples"
# panel renders the live BRIGHTDATA_API_KEY as a Bearer token.
m2dbidconfiguration/
fb1.png

# AR-1 admin UI local config — holds the Supabase service_role key, which bypasses
# RLS. config.example.js is the committed template.
admin/config.js
