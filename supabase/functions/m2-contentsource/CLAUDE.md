# m2-contentsource — M2 Content Sources (material, not signal)
- Only writer of `source_events`. Never read/write another domain's tables (D12).
- Every third-party call goes through guardedFetch. No budget row = denied.
- reference_post is inspiration-tier: text never verbatim, media only inside
  transformative composition (SourceEnriched v1 Rule 3, amended 2026-07-18).
- harvest_plan scope "due" CLAIMS AND ADVANCES rows — it is not a dry run.
- READ readme.md in this folder before editing — it holds the paid-for lessons
  (field_map verification, external_id, collect-vs-discover).

## Learned rules
(appended only after a mistake's fix is proven and Raze approves — see root CLAUDE.md)
